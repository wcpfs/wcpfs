#!/usr/bin/env bash

root_dir=`dirname $0`/..

server=scheduler
remote_deploy_url=$server:git/wcpfs.git

if [[ ! -n $(git status --porcelain) ]]; then
  echo "Deploying to $remote_deploy_url"
  git push $remote_deploy_url master
else
  git status
fi
